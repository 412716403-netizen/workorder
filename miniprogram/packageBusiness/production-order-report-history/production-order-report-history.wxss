@import '../../styles/plan-list-shell.wxss';

.report-history-page {
  padding: 0 var(--spacing-page) calc(120rpx + env(safe-area-inset-bottom));
}

.report-history-filter {
  flex: 1;
  padding: 16rpx;
  background: var(--color-bg-card);
  border-radius: 12rpx;
  font-size: 24rpx;
  color: var(--color-text-secondary);
  text-align: center;
}

.report-history-filter--picker {
  color: var(--color-primary);
  font-weight: 600;
}

.report-history-row {
  padding: 24rpx;
  margin-bottom: 16rpx;
  background: var(--color-bg-card);
  border-radius: 16rpx;
}

.report-history-row__body {
  display: flex;
  gap: 20rpx;
  align-items: flex-start;
}

.report-history-row__left {
  width: 112rpx;
  flex-shrink: 0;
}

.report-history-row__thumb {
  flex-shrink: 0;
}

.report-history-row__img {
  width: 112rpx;
  height: 112rpx;
  border-radius: 16rpx;
  border: 1rpx solid var(--color-border);
  background: var(--color-bg-page);
  display: block;
}

.report-history-row__img--placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  background: #eef2ff;
  border-color: #e0e7ff;
}

.report-history-row__placeholder-icon {
  width: 48rpx;
  height: 48rpx;
}

.report-history-row__content {
  flex: 1;
  min-width: 0;
}

.report-history-row__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  margin-bottom: 10rpx;
}

.report-history-row__head-main {
  display: flex;
  align-items: center;
  gap: 8rpx;
  min-width: 0;
  flex: 1;
}

.report-history-row__order-no {
  font-size: 22rpx;
  font-weight: 500;
  color: var(--color-text-secondary);
  flex-shrink: 0;
}

.report-history-row__head-dot {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  flex-shrink: 0;
}

.report-history-row__time {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
  min-width: 0;
}

.report-history-row__report-no {
  font-size: 20rpx;
  color: var(--color-text-tertiary);
  flex-shrink: 0;
}

.report-history-row__main {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

.report-history-row__milestone {
  flex: 1;
  min-width: 0;
  font-size: 30rpx;
  font-weight: 700;
  color: var(--color-text-primary);
}

.report-history-row__qty {
  flex-shrink: 0;
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-primary-dark);
}

.report-history-row__aside {
  margin-left: auto;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4rpx;
}

.report-history-row__product {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12rpx;
  margin-bottom: 8rpx;
}

.report-history-row__product-left {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  align-items: baseline;
}

.report-history-row__product-name {
  font-size: 26rpx;
  color: var(--color-text-primary);
}

.report-history-row__sku {
  font-size: 22rpx;
  color: var(--color-text-tertiary);
}

.report-history-row__meta {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  align-items: baseline;
  font-size: 22rpx;
}

.report-history-row__operator {
  flex-shrink: 0;
  font-size: 20rpx;
  color: var(--color-text-tertiary);
  text-align: right;
  max-width: 200rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.report-history-row__defective {
  flex-shrink: 0;
  font-size: 20rpx;
  color: #d97706;
  font-weight: 600;
  text-align: right;
}

.report-history-footer {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 20rpx var(--spacing-page) calc(20rpx + env(safe-area-inset-bottom));
  background: var(--color-bg-card);
  border-top: 1rpx solid var(--color-border);
  box-shadow: 0 -8rpx 24rpx rgba(15, 23, 42, 0.06);
}

.report-history-footer__text {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--color-text-secondary);
}
