---
description: 小程序列表/流水/清单中产品编号与名称展示约定
globs: miniprogram/**/*
alwaysApply: false
---

# 小程序列表 · 产品编号 + 名称

凡**流水、清单、主列表卡片、产品选择弹窗**展示成品/产品时：

1. **首行**：产品编号（`Product.name`）大字粗体 + 产品名称（`Product.sku`）小字灰色
2. **次行**：合作单位（分类 `linkPartner` + 已关联时）+ 分类自定义字段 pill

产品选择弹窗用 `searchable-product-select`，须传 `partners="{{partners}}"` 才出合作单位 pill。

## 数据层（必做）

- 使用 [`listProductThumb.js`](../miniprogram/utils/listProductThumb.js)：
  - `listProductNameSkuFields(product, fallback?)` → `{ productName, productSku, showProductSku }`  
    - `productName` = 编号（name），无编号时退回名称  
    - `productSku` = 名称（sku），仅编号与名称皆有且不同时展示  
  - `listProductMetaFields(product, category, partnerNameById)` → 合作单位 + 自定义 tags  
  - 有 `productMap` 时用 `listProductNameSkuFromMap` / `listProductDisplayFieldsFromMap` / `listProductMetaFieldsFromMaps`
- 原始字段拆分仍用 `productNameSkuParts`：`name`=编号、`sku`=名称（业务逻辑用，勿直接当列表标题）
- 列表搜索须同时覆盖名称与编号

## bootstrap 装配（必做）

列表页要拉 `productMap` / `categoryMap` / `partnerNameById` 时，用
[`productMetaMaps.js`](../miniprogram/utils/productMetaMaps.js) 的 `loadProductMetaMaps()`，
别再各页手写 `Promise.all([fetchProductsAll, fetchCategoriesAll, fetchPartnersAll])` + `new Map(...)`：

```js
const { loadProductMetaMaps } = require('../../utils/productMetaMaps.js');

const [records, productMeta] = await Promise.all([fetchRecords(), loadProductMetaMaps()]);
this._productMap = productMeta.productMap;
this._categoryMap = productMeta.categoryMap;
this._partnerNameById = productMeta.partnerNameById;
```

- 自己已拉过产品的页面传 `loadProductMetaMaps({ withProducts: false })`
- 三个请求都带 90s 缓存；**维护页**（改完要立刻看到新值）请直连各自 `*Api.js` 的未缓存版本

## 视图层（WXML）

用模板 [`templates/listProductTitle.wxml`](../miniprogram/templates/listProductTitle.wxml)，不要复制结构：

```xml
<import src="/templates/listProductTitle.wxml" />
...
<template is="listProductTitle" data="{{item: line}}" />
<template is="listProductMeta" data="{{item: line}}" />
```

标题行内要额外塞徽章 / 数量（工单中心、产品档案、款式列表）时，标题保持页面内联，
元信息行仍走 `listProductMeta`。

全局样式：[`styles/list.wxss`](../miniprogram/styles/list.wxss) 中 `.list-product-title*` / `.list-product-meta*`。

## 禁止

- 把 `product.sku`（名称）写成大标题而不走 `listProductNameSkuFields`
- 用 `wx:if="{{item.productSku}}"` 代替 `showProductSku`
- 各页自创「名称+编号」拼接字符串
- 复制 `list-product-meta` 那段 WXML 而不用 `listProductMeta` 模板
- 自定义字段字段名改叫 `customTags`：统一 `productCustomTags` / `showProductMeta`

完整说明见 [`docs/10-miniprogram-ui.md`](../docs/10-miniprogram-ui.md) §产品编号与名称。
