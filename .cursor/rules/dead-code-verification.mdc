---
description: 判定「死代码 / 不可达分支」前的强制复核步骤
alwaysApply: true
---

# 死代码判定：先复核，再下结论

**不要凭一次搜索就断言某段代码不可达。** 误判会导致删掉线上主路径。

## 下结论前必须走完

1. **穷举命名变体**：搜到 `onSave=` 无结果 ≠ 没接线。同时搜前缀/后缀变体：
   `onSave` / `onSaveManual` / `onSaveBatch` / `handleSave*`，以及只搜标识符本身（不带 `=`）。
2. **两头对撞**：既从**定义处**搜调用点，也从**渲染/接线处**（JSX props、路由表、事件注册、配置映射）反查。
3. **查间接引用**：字符串键调用、`Record<string, fn>` 映射表、re-export、动态 `import()`、
   同名不同层的函数（父组件原始 prop vs JSX 内联包装版，二者不是同一个函数）。
4. **确认触发入口**：找到实际能点到它的 UI 按钮 / 接口 / 定时任务，而不只是找到一个函数引用。

## 表述要求

- 复核后仍无法证明可达 → 说「**未找到调用点**，建议确认」，**不要**说「这是死代码」。
- 证据要具体到文件与行号，让人能自己复查。

## 真实教训

`OrderBillFormPage.tsx` 的 `handleSaveManual('PURCHASE_BILL')` 分支曾被误判为死代码：
grep 用了 `onSave=`，漏掉了实际接线的 `onSaveManual=`（第 1546 行），
而它正是「确认保存采购入库」按钮的主保存路径。
同时该文件里 `onSaveBatch` 有两个不同实例（组件 prop 与传给子组件的内联 wrapper），
只看名字会误判为重复调用。
