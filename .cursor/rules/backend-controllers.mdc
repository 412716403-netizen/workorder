---
description: 后端 controller 层约定：HTTP 映射，业务下沉到 service
globs:
  - backend/src/controllers/**/*.ts
---

# 后端 controllers/ 约定

controller 文件**只做**：
1. 用 `asyncHandler` 包装，避免散落 `try/catch`。
2. 从 `req` 取 `tenantId`、`params`、`query`、`body`。
3. 通过 `getTenantPrisma(req.tenantId!)` 拿租户 Prisma。
4. 调用 `services/*.service.ts` 中的纯业务函数。
5. `res.json(...)` 或 `res.status(201).json(...)` 返回结果。

## 必做

- 所有导出处理函数用 `asyncHandler(async (req, res) => { ... })`（`backend/src/middleware/asyncHandler.ts`）。
- 取 `tenantId`：`const tenantId = req.tenantId!;` 然后 `const db = getTenantPrisma(tenantId);`。
- 参数清洗用 `utils/request.ts` 的 `str / optStr / sanitizeCreate / sanitizeUpdate / normalizeDates` 等（保持与现有 controller 一致）。
- 错误抛 `AppError`（`backend/src/middleware/errorHandler.ts`）而不是 `res.status().json()`；让全局 `errorHandler` 统一返回。

## 禁止

- 写循环/分支实现业务规则（→ 下沉到 service）。
- 在 controller 里写原始 Prisma 查询或事务。
- 手动 `try { ... } catch { res.status(500)... }`（用 `asyncHandler` + `AppError`）。
- 依赖 `localStorage` / 前端状态概念。

## 参考模板

```ts
import { getTenantPrisma } from '../lib/prisma.js';
import { str, optStr } from '../utils/request.js';
import * as svc from '../services/xxx.service.js';
import { asyncHandler } from '../middleware/asyncHandler.js';

export const list = asyncHandler(async (req, res) => {
  const db = getTenantPrisma(req.tenantId!);
  const result = await svc.list(db, {
    search: optStr(req.query.search),
    page: req.query.page ? Number(req.query.page) : undefined,
  });
  res.json(result);
});

export const create = asyncHandler(async (req, res) => {
  const tenantId = req.tenantId!;
  const db = getTenantPrisma(tenantId);
  const record = await svc.create(db, tenantId, req.body);
  res.status(201).json(record);
});
```

参考现成文件：`backend/src/controllers/plans.controller.ts`。
